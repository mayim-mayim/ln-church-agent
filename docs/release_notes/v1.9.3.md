# Release v1.9.3 — MCP Registry Namespace Fix

This is a hotfix release to align the package namespace for publication to the Official MCP Registry.

## 🔧 Fixes
* **MCP Registry Compatibility**: Updated the `mcp-name` identifier in the `README.md` and `server.json` from `io.github.mayim364` to `io.github.mayim-mayim` to pass strict PyPI package ownership validation. No functional code changes were made to the SDK or the inspect-only MCP entrypoint.